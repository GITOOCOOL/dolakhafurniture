FROM node:22-alpine

WORKDIR /app

# Install dependencies first for better caching
COPY package*.json ./
RUN npm install

# Copy the rest of the application
COPY . .

# Expose the sanity development port
EXPOSE 3333

# Start the dev server
CMD ["npm", "run", "dev"]
